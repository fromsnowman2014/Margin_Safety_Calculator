import type { Metadata } from 'next';
import './globals.css';
import { Header } from '@/components/layout/Header';
import { Footer } from '@/components/layout/Footer';

export const metadata: Metadata = {
  title: 'SafetyNet - Graham\'s Margin of Safety Calculator',
  description: 'Calculate the intrinsic value and margin of safety of stocks using Benjamin Graham\'s proven formula',
  keywords: ['value investing', 'margin of safety', 'Benjamin Graham', 'stock valuation', 'intrinsic value'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="font-sans antialiased">
        <div className="flex flex-col min-h-screen">
          <Header />
          <main className="flex-1">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  );
}
